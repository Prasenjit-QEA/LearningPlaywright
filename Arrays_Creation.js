//Array

let fruites = []; // Empty []
let fresh_fresh = ["apple", "banana", "cherry"];


let mixed = [1, "hello", true, null]; //JS arrays can hold any type


console.log(mixed[4]);
console.log(mixed[2]);
console.log(mixed.length);//length is a property it not a function ().


let secore = new Array(3); //create [empty x 3]

//also can be create with

let test = Array.of(10, 20, 30, 50);

//Array.from()
let chars = Array.from("hello");//"h","e","l","l","o"